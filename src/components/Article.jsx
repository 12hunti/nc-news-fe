function Article(){}

export default Article

{/* 
     const [vote, setVote] = useState(0)
    <div className="card">
<button onClick={() => setCount((vote) => vote + 1)}>
  vote is {vote}
</button>

</div> */}